const fangFa = () => {
    if (value) {
        if (value !== undefined && value !== null && value !== '') {
            return value
        } else {
            return '--'
        }
    }
};
export default fangFa;